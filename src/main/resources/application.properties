spring.application.name=servicio-productos
#server.port=8001
server.port=${PORT:0}
eurka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}

aureka.client.service-url.defaultZone=http://localhost:8761/eureka
#aureka.client.service-url.defaultZone=http://servicio-eureka-server:8761/eureka

spring.sleuth.sampler.probability=1.0
